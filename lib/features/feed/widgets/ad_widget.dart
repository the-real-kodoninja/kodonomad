import 'package:flutter/material.dart';

class AdWidget extends StatelessWidget {
  const AdWidget();

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      title: Text('Ad Placeholder'),
      subtitle: Text('Sponsored Content'),
    );
  }
}
